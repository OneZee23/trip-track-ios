# 0.6.6 «Путешествие» — что нажать

Код на ветке, тесты зелёные. Порядок тот же, что в 0.6.5: **бэкенд раньше
сабмита**, тексты стора во все локали карточки, рейтинг 13+ уже стоит.

| Что | Где | Состояние |
|---|---|---|
| iOS `release/0.6.6` | github.com/OneZee23/trip-track-ios | коммиты `6c3318a..HEAD` (11 задач), **не запушено** |
| Бэкенд | gitlab.com/triptrack1/triptrack-backend | модуль `journeys`, миграция `1788600000000-AddJourneys`; коммиты `0630c40`, `c26ad58`, `f8c0269` на `master`, **не запушены** |
| Версия | `project.yml` | 0.6.6, билд **58** |
| Схема CoreData | `TripTrack.xcdatamodeld` | **v12** — `JourneyEntity`; после добавления версии `xcodegen generate` ДВАЖДЫ |
| Тесты | локально | 907 iOS · 380 бэкенд |
| Спека | `docs/superpowers/specs/2026-09-08-journeys-design.md` | решения приняты 8 сен |
| План | `docs/superpowers/plans/2026-09-09-journeys-066.md` | 11 задач, все закрыты |
| Макеты | Figma `8AlZTuVAZueffBsXs0QBuz`, страница `2848:1307` | A и C — канон |

---

## 1. Бэкенд — сначала он

Клиент 0.6.6 ходит в `POST /journeys/upsert|list|delete` и ждёт секцию
`journeys` в `/sync/pull`. Старый сервер вернёт 404 на новые маршруты и просто
не отдаст секцию — приложение это переживёт, но путешествие не доедет до
второго телефона. Деплой до сабмита — чтобы фича работала с первого дня.

- [ ] Два коммита (`0630c40` модуль + миграция, `c26ad58` «отсутствующий ключ
      не сбрасывает поле») **запушить в `master`** — CI собирает и выкатывает
      только с него.
- [ ] В выкатке едет миграция `AddJourneys`: таблица `journey` с FK на
      `account(id)` ON DELETE CASCADE и индексом `idx_journey_account`.
      Идемпотентна — `CREATE TABLE IF NOT EXISTS`.
- [ ] Проверка живого маршрута (любой свой аккаунт, путешествий может не быть):
      ```
      curl -s -X POST -H "Authorization: Bearer $TOKEN" \
        -H 'Content-Type: application/json' -d '{}' \
        https://api.trip-track.app/journeys/list | jq
      ```
      Ждём `{"journeys": []}` — не 404 и не 500.
- [ ] Проверка синка: `POST /sync/pull` с `entityTypes: ["journey"]` отдаёт
      `journeys: {upserted: [], deleted: []}`. Без правки whitelist в
      `sync-pull.dto.ts` этот запрос падал бы на валидации — она в коммите.

## 2. iOS — пальцем на телефоне

Автотесты закрывают правила; ниже то, что видит только человек.

- [ ] **Создание с экрана поездки.** «…» → «Объединить в путешествие»: в листе
      соседи ±7 дней, все отмечены, галочки снимаются. «Создать путешествие» →
      тост «Путешествие создано».
- [ ] **Создание из «Мои».** Долгое нажатие на карточку поездки → лист
      объединения (те же соседи ±7 дней); прокрутка списка не спотыкается о
      карточку под пальцем.
- [ ] **Экран путешествия.** Карта-склейка всех плеч с кружками отметок; четыре
      плитки итога; дни с плечами; стоянка «Тбилиси · 5 поездок по городу»
      раскрывается по нажатию.
- [ ] **Правка дат с пересечением** даёт красную ошибку `journeyOverlaps`, а не
      молчаливый отказ и не второе путешествие поверх первого.
- [ ] **Удаление путешествия оставляет поездки**: подтверждение → в «Моих» на
      месте карточки снова стоят все плечи.
- [ ] **Второй телефон.** С Cloud Sync путешествие приезжает целиком (имя,
      окно дат, исключённые поездки); правка на первом телефоне, ещё не
      уехавшая, не затирается входящим pull.
- [ ] **`LocalDataWipe` и удаление аккаунта** стирают `JourneyEntity` — у неё
      нет связи с поездкой, каскад её не заберёт.
- [ ] **«Это твой дом?»**: «Нет» — карточка исчезает СРАЗУ и не возвращается в
      этом запуске (вернётся через 30 дней); «Да» закрывает вопрос навсегда.
- [ ] **Джанхот туда-обратно в один день** подсказку НЕ вызывает: путешествие
      делает ночь не дома, а не 200 км.
- [ ] **Прогнать `JourneySuggester` по своей базе: после Джанхота (6 сен)
      подсказки нет; после Тбилиси — есть.**

### Полевая проверка финального ревью

- [ ] Composer: untick a middle neighbour → the trip is NOT a leg.
- [ ] Edit → cover → card and hero show the photo.
- [ ] Second phone with Cloud Sync: card appears after pull without relaunch.
- [ ] Delete all legs → empty card remains, can be deleted.
- [ ] Edit sheet → «Удалить путешествие» → no stuck sheet during the pop.
- [ ] Cold cache: stay card heading reads «Стоянка».
- [ ] Long-press a card: one haptic; release does not open the trip.
- [ ] 430-pt phone: selection bar width; dark theme day badge.
- [ ] Карточка путешествия со стоянкой: подпись «N поездок · M по городу».
- [ ] Пустое путешествие: плашка показывает настоящие дни окна, не «1 день».
- [ ] Turkish: uppercase pill/badges.
- [ ] Home card with empty cache shows coordinates — acceptable?

## 3. Сабмит

1. `xcodegen generate` **дважды** и проверить `.xccurrentversion` = `TripTrack v12`
   (первый прогон после новой версии модели откатывает файл — см. CLAUDE.md).
2. Xcode → Product → Archive → Distribute → App Store Connect.
3. «What's New» во **все** локализации карточки из [app-store.md](app-store.md)
   — тринадцать блоков. Сабмит 0.6.2 отклонили ровно за одну пропущенную.
4. Notes для ревьюера — блок 0.6.6 из [app-review-notes.md](../app-review-notes.md).
   Там же ответ про «дом»: выводится на телефоне, хранится локально, никуда не
   отправляется.
5. Скриншоты: экран путешествия — лучший кадр релиза.

## 4. После аппрува

- [ ] Тег `v0.6.6` на коммит билда 58, ветка в `master`.
- [ ] Пост в канал: [tg-post.md](tg-post.md).
- [ ] Бэкенд-тег и запись в инвентаре серверов, если выкатка что-то поменяла.

## Известные мелочи — закрыто волной 2

Весь список deferred-мелочей закрыт одной волной правок (`fix: мелочи 0.6.6`):
общий `JourneyTitle`, полилинии через `NSCache`, `trips(in:)` предикатом,
`LazyVStack` в ленте дней, один обход safe-area на три экрана, отмена очереди
синка при удалении, `pendingDelete` не воскресает пулом. Тестов стало 907.

Осталось ЖИТЬ как есть (решения, а не долг):

- **`legCount` не считает местные поездки** — теперь это видно: подпись
  карточки читается «6 поездок · 2 по городу».
- **Опорная поездка стоит в листе первой**, ломая хронологию: человек пришёл
  с её экрана.
- **Переводы `journeyLegsLabel`/`journeyDrivingLabel`** (IT «Tratte»,
  PT «Trechos») проверены чтением, не носителем.
- **«Нет» на вопрос о доме держится 30 дней**, а не навсегда
  (`JourneySuggester.homeReaskDelay`): задать дом руками в 0.6.6 нечем, и
  вечное «нет» отрезало бы человека от подсказок из-за одной ранней ошибки
  вывода.
