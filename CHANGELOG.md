# Changelog

Все заметные изменения в проекте документируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/).

---

## [0.6.0] -- 22 апреля 2026

Социальный слой поверх синка v0.5.0: лента поездок, профили, реакции, шеринг, репорты/блоки. Плюс модель приватности "по умолчанию приватно" для всех поездок.

### Добавлено

**Приватность поездок**
- **Приватно по умолчанию** — все новые поездки помечаются `isPrivate = true` при записи. Публикация — явный toggle из деталей поездки
- **Миграция существующих поездок** — one-time `CoreDataTripRepository.migrateAllTripsToPrivate()` при апгрейде до v0.6.0 переводит все имеющиеся поездки в приватные и помечает под повторный sync. Ничего не утекает в ленту без согласия пользователя
- **Optimistic privacy flip** — при переключении поездки обратно в приватные из деталей, `SocialFeedStore.removeOptimistically()` сразу убирает карточку из ленты, не дожидаясь round-trip до сервера. Через 1.5с — refresh для выравнивания с серверным состоянием
- **`.tripPrivacyChanged` нотификация** с `PrivacyChangePayload` — механизм реакции UI на изменения видимости

**Лента и карточки**
- **Paged feed tabs** — вкладки "Лента" ↔ "Мои" теперь горизонтальный swipe через нативный `TabView(.page)` с iOS-физикой. Пиллы сверху закреплены, только контент слайдится
- **Объединённая лента "Лента"** — публичные поездки тех на кого подписан + "Предложенные" пользователи карусель сверху, когда подписок мало. Strava-стиль карточек (`SocialFeedCardView`) с author row, map preview, метриками, pill-ами реакций и share-кнопкой
- **Author row tap** — тап на автора карточки открывает его публичный профиль. Тап на саму карточку — детали поездки (`SocialTripDetailView`)
- **Auto refresh on appear** — лента подтягивается при каждом открытии таба + при `.syncPullCompleted` и `.tripRecordingEnded`

**Публичные профили (v0.6 редизайн)**
- **Обогащённый `PublicProfileView`** — hero с баннером, аватаром, именем и рангом (Новичок / Водитель / ...), стрик с 🔥 в stats grid, карточка активной машины (`Break-in LVL 2` + одометр), горизонтальный ряд последних 6 ачивок, followers/following, последние поездки
- **Фоны профиля (`ProfileBackground`)** — 8 градиентных вариантов выбираемых в `ProfileBackgroundPickerSheet`, синкаются на сервер и рендерятся в баннере hero
- **"Посмотреть как видят другие"** — кнопка в `ProfileView` открывает свой же `PublicProfileView` в шите. Автоматический push профиля на сервер при открытии — чинит случаи когда `displayName` не доехал на сервер из Apple Sign-In
- **Fallback имени** — для собственного профиля если server вернул `displayName=null`, показывается имя из Keychain Apple Sign-In
- **Profile sync расширен** — `ProfileUpdateRequest` теперь везёт `profileLevel / profileXp / currentStreak / bestStreak / activeVehicleId` вдобавок к имени/аватару/фону. Бэкенд-ответ `/user/profile/:id` отдаёт `currentStreak`, `bestStreak`, `activeVehicle {name,level,odometerKm,avatarEmoji}`, `recentBadges: string[]` (агрегация из `Trip.badgesJson`)
- **Hero profile card** — большая карточка сверху `ProfileView` с аватаром, именем, email и индикатором sync-статуса
- **Social counters** — followers/following прямо в `ProfileView`, тап открывает `FollowListView`

**Реакции**
- **Long-press для реакций** — долгий тап на карточке открывает емодзи-палитру, короткий тап — открывает детали
- **Per-emoji pills** — вместо одного счётчика реакций показываем топ-3 самых популярных с количеством, `+` открывает палитру. Своя реакция выделена цветом
- **Car-themed набор** — `🔥 🏁 🏎️ 🛣️ 🗺️` (заменён generic набор 👍❤️...)
- **Breakdown в деталях поездки** — полный список реакторов (`SocialReactionEntry`), тап на реактора — его публичный профиль

**Шеринг и stories**
- **Story-style share sheet** — `StoryShareSheet` рендерит карточку поездки 9:16 с картой, метриками и брендом, экспорт в Instagram/галерею. Gracefully работает в guest mode
- **Share link через бэкенд** — `POST /social/share` возвращает `shareCode`, ссылка `https://trip-track.app/s/<code>`. Контроль presentation chain (`fcc9fc7`) для корректной работы из full-screen экранов

**Модерация**
- **Block/unblock** — действие из профиля и меню карточки. Бэкенд удаляет все follow-связи в обе стороны, скрывает контент из лент/поиска/suggested
- **Список заблокированных** (`BlockedListView`) из профиля с confirm-диалогом для отзыва блокировки
- **Report** (`ReportSheet`) — репорт на пользователя или поездку с причиной (spam / harassment / hate / nudity / violence / illegal / impersonation / other) + заметка
- **Content filter на названия** (`ContentFilter`) — пре-модерация заголовков поездок перед публичным показом

**Discover**
- **`DiscoverView`** — поиск пользователей по имени + suggested карусель (топ пользователей по `profileLevel`, исключая тех на кого уже подписан и заблокированных)

**Прочее**
- **Unified back button** (`NavBackButton`) — единый кастомный back-button без "Back"-текста через всё социальное навигационное дерево (`276b204`)
- **Hero profile card** (`521f491`) — большая верхняя карточка `ProfileView` заменила скромный заголовок
- **Account actions в CloudSync** — "Выйти" и "Удалить аккаунт" теперь лежат в `CloudSyncView`, убраны из корня профиля

### Исправлено
- **Auto refresh ленты** после возврата в таб (`0e6712a`) — раньше надо было pull-to-refresh вручную
- **Грамматика consent-текстов** и отступы сверху в деталях поездки (`4cf72aa`)
- **Cancel stale refresh** — если пользователь быстро переключает табы, предыдущий `fetchPage` отменяется через `Task.checkCancellation()` (`88d0993`)
- **Share sheet presentation chain** — больше не теряется при открытии из full-screen модала (`fcc9fc7`)
- **Purge orphan trips** — сервер чистит "висящие" поездки без валидной связи (`aaa6b43`)
- **Stationary tail trim** — хвост "стояния" в конце поездки отбрасывается при финализации (`8d723a1`)

### Архитектура данных
- **Локальные данные — источник правды** (философия "лучше чем Google Timeline"): сервер в ответе `/sync/pull` теперь отдаёт `ownedCounts: {trips, vehicles, photos}` — сколько non-deleted сущностей у него числится. Если клиент видит что у сервера меньше чем помечено `synced` локально — запрашивает новый `POST /sync/manifest` (только UUID-ы), находит пропавшие у сервера записи и помечает их `pendingUpload`. Очередь автоматически пере-заливает. Если сервер умрёт и будет восстановлен из пустоты — данные поднимутся обратно с клиентов, никто ничего не потеряет
- **Server-side бан пользователя**: `AccountEntity.isBanned` флаг. `JwtAuthGuard` и `/auth/refresh` / `/auth/login` возвращают 403 `USER_BANNED` для забаненных. Клиент `APIClient` при получении этого кода шлёт нотификацию `.userBanned` → `AuthService` делает `signOut()`. Локальные CoreData данные **остаются нетронутыми** — забаненный юзер может смотреть свои поездки read-only, просто без sync. Все его попытки re-upload блокируются

### Безопасность
- **Stored XSS на `/s/:code` share landing**: поля `title / region / displayName / avatarEmoji` интерполировались в HTML без экранирования. Пользователь с именем `<script>...</script>` мог угнать токены у любого кто открыл share-ссылку. Экранирование + `Content-Security-Policy: default-src 'none'` через extracted `escapeHtml` + `renderSharedTripHtml` функции
- **`UserNotAuth` пропагируется корректно**: добавлен глобальный `AppErrorFilter` — если `@CurrentUser()` бросает `UserNotAuth` вне `PostApiEntry` роута (где есть `JsonRpcExceptionFilter`), ответ идёт как `401 JSON` вместо raw 500 со стэк-трейсом
- **`/users/:id/profile` защищён**: добавлены `@UseGuards(JwtAuthGuard, ThrottlerGuard)` + `@CurrentUser()` декоратор (проверяет подпись JWT через guard, а не `jwt.decode`). Неавторизованные запросы теперь отвергаются. Rate limit 60 запросов/мин на IP
- **Блоки уважаются в профиле**: `getProfile` возвращает `UserNotFound` если между запрашивающим и целевым аккаунтом есть блок в любую сторону. Ошибка одинакова для блока и несуществования, чтобы не подтверждать факт блокировки
- **`isPublic` уважается**: non-public аккаунт видим только владельцу и активным подписчикам. Остальным — `UserNotFound`
- **Ownership check на `activeVehicleId`**: `AuthService.updateProfile` теперь проверяет что UUID машины принадлежит аккаунту запрашивающего. Раньше можно было выставить чужой `vehicle.id` в свой профиль и "карточка Your car" показывала бы чужую машину (имя / одометр / уровень). `getActiveVehicle` также дополнительно фильтрует по `accountId` defence-in-depth
- **`IsUUID()` + `ValidateIf` на `activeVehicleId`**: пустая строка как "очистить выбор" теперь проходит валидацию. Раньше class-validator отвергал с 400
- **Клиентские game-state поля ограничены**: `profileLevel ≤ 100`, `profileXp ≤ 10M`, `currentStreak/bestStreak ≤ 10k`. Вдобавок `profileLevel` убран из `ORDER BY` в suggested-users — чтобы читер с profile-update push'ем `999` не мог закрепиться в топе
- **`parseBadgeIds` валидирует shape**: JSON.parse результат проверяется на `Array<string>` + каждый id матчится `[a-z0-9_]{1,40}`. Defence-in-depth против crafted `badgesJson` push'ей

### Изменено
- **Бэкенд**: `AccountEntity` получил `active_vehicle_id` (uuid, nullable); `currentStreak / bestStreak` уже были. `ProfileUpdateRequestDto` принимает новые опциональные поля. `SocialService.getProfile()` джойнит активную машину (с ownership-check) и агрегирует бейджи из последних 50 поездок
- **`AuthService.updateProfile` — единственный writer для profile-mirrored полей** на `AccountEntity`. Раньше `SettingsService.upsert` тоже зеркалил level/streak/avatar в `AccountEntity` — два writer-а без координации `updatedAt` приводили к stale clobbering. Mirror убран
- **Клиентский `ProfileUpdateRequest` использует `encodeIfPresent`**: nil-поля теперь пропускаются из JSON вместо отправки `null`. Раньше отправка `displayName: nil` стирала имя на сервере
- **`PublicProfileView` сбрасывает gate при смене auth**: `onChange(of: auth.isSignedIn)` обнуляет `didInitialLoad`, чтобы после sign-out/sign-in view не залипал на данных предыдущего аккаунта
- **`toggleFollow` при ошибке теперь перечитывает профиль** вместо отката к pre-optimistic snapshot — снимок мог быть устаревшим если параллельный pull уже обновил `profile`
- **`toolbarBackground(.visible)` на PublicProfileView + FollowListView**: фиксит "teleport" артефакт — во время pop-анимации nav bar больше не становится прозрачным и не просвечивает предыдущий экран
- **`getMyReactions` больше не N+1**: предикат `tripId: In(tripIds)` пушится в query, вместо fetch-all-then-filter-in-JS
- **SyncEnqueuer моментально кикает очередь** после `enqueue` (раньше приходилось ждать 5-мин таймер). Toggling приватности поездки теперь доходит до сервера за секунды
- **Orphan trip delete** — `APISyncTransport.deleteTrip` теперь проглатывает `tripNotFound` как успех, чтобы очередь не ретраила удаление уже отсутствующей на сервере поездки
- **Pull queries** — `OR` в where-клаузах `sync.service` обёрнуты скобками (precedence bug: без скобок фильтр по `accountId` шорткатился)
- **Русский язык** в социальных экранах — везде на "Вы"
- **Legal hardening** — privacy policy и terms обновлены под социальные фичи (`1337061`)

---

## [0.5.0] -- 20 апреля 2026

### Добавлено
- **Собственный backend-сервер** (NestJS + PostgreSQL + JWT + Cloudflare R2). Репозиторий `trip-track-backend`. API для auth, trips/vehicles CRUD, photos, sync, social (follows, feed, reactions, sharing)
- **Клиентская интеграция с сервером**. `APIClient` (URLSession + JSON-RPC envelope + single-flight token refresh), `APISyncTransport` (реализует существующий `SyncTransport`), `R2PhotoStorage` (реализует `RemotePhotoStorage`), `SyncCoordinator` (триггеры: foreground, network restored, Wi-Fi connected, 5-мин таймер), `PullApplier` (merge server delta в CoreData)
- **Sign in with Apple на сервере**. После Apple Sign In клиент вызывает `/auth/login` → сервер верифицирует identity token через Apple JWKS → выдаёт свой access+refresh JWT. Хранятся в Keychain
- **Умная загрузка фото**. Thumbnails всегда (даже на cellular), оригиналы только на Wi-Fi. Pending originals автоматически заливаются когда появляется Wi-Fi. Presigned URL-ы для чтения (1ч TTL)
- **Conflict resolution**. Optimistic concurrency через `conflictVersion` + `lastModifiedAt`. Silent last-write-wins для MVP
- **First sync после Sign in**. Все локальные поездки, vehicles, settings, фото автоматически попадают в очередь и уезжают на сервер
- **Guest mode сохранён**. Без Sign in приложение работает как раньше — полностью локально. Sync — opt-in
- **Тоггл синхронизации** (`CloudSyncView`) — отдельный экран из профиля: включить/выключить cloud sync, индикатор статуса, карточки с объяснением что синхронизируется и где хранится. Выключение блокирует `SyncEnqueuer` глобально
- **Прогресс синхронизации X/Y** — `SyncQueue.batchTotal` + `batchProcessed` показывают сколько сущностей осталось обработать. Видно в ProfileView и CloudSyncView
- **Восстановление после перезапуска** — `SyncCoordinator.recoverPendingEntities()` при старте сканирует CoreData на `syncStatus=pendingUpload` и перезапускает очередь. Если приложение убили в процессе синка, при следующем запуске всё доедет
- **Экспорт логов** (`DebugLogsView`) — кнопка в профиле, читает `OSLogStore` по подсистеме `com.triptrack`, хранит 48 часов, экспорт как текстовый файл через `ShareLink`. В начале файла блок идентификации (local_user_id, account_id, signed_in, sync_enabled) для отладки по письму от пользователя
- **Единый стиль close button** — компонент `SheetCloseButton` (`xmark.circle.fill`, серый, справа вверху). Применён ко всем шитам: CloudSync, DebugLogs, Stats, Badges, Garage, AddVehicle, BluetoothScan, DevMenu, RoadCollection, VehicleDetail

### Изменено
- CoreData schema v2 → v3 (добавлены `thumbnailURL` на фото, `conflictVersion` на vehicle/settings). Lightweight migration
- `NetworkMonitor` теперь публикует `isOnWiFi`. `CacheManager` — новый publisher `wifiConnected`
- **Миграция `print()` → `Logger`** в SyncCoordinator, AuthService, ProfileView — чтобы логи попадали в OSLogStore и были доступны для экспорта
- **Feed автообновляется после pull** — `PullApplier` шлёт `.syncPullCompleted` нотификацию, `FeedViewModel` перезагружает список
- **Русский язык — уважительно на "Вы"** везде (AppStrings, CloudSyncView, DebugLogsView)
- **Сломанные поездки компактнее** — карточки с `previewCoordinates.count ≤ 1` больше не показывают пустую карту-плейсхолдер, только статистику
- Версия 0.4.4 → 0.5.0, build 10 → 11

### Удалено
- `SettingsView.swift` и папка `Views/Settings/` — единственное место для пользовательских настроек теперь `ProfileView`. `BluetoothScanSheet` перемещён в `Views/Profile/`

---

## [0.4.4] -- 19 апреля 2026

### Исправлено
- **Ложные "пешие" поездки в auto-режиме**. `CMMotionActivityManager` иногда post-hoc классифицирует активность как `automotive.high` с задержкой — например, когда человек уже вышел из машины и идёт пешком. Такая запись могла висеть до ~23 минут (20 мин GPS-inactivity + 3 мин таймер) и попадала в ленту как "поездка" с max 5-11 км/ч. Расширен junk-фильтр: теперь при остановке записи поездка удаляется, если `max speed < 15 км/ч AND duration > 3 мин`. Не затрагивает легитимные поездки (где max всегда ≥15) и короткие манёвры на парковке (уже ловятся старым условием `distance < 500 м AND duration < 2 мин`)
- **Лента не обновлялась после авто-записи в фоне**. Если приложение записывало поездку, пока пользователь его не трогал, при следующем открытии лента оставалась пустой до ручного pull-to-refresh. Добавлена `tripRecordingEnded` нотификация, `FeedViewModel` подписан и перезагружает список автоматически

### Изменено
- Версия 0.4.3 → 0.4.4, build 9 → 10

---

## [0.4.3] -- 18 апреля 2026

### Исправлено
- **Ложное автозавершение поездки в auto-режиме** — CMMotion давал событие `automotive → not automotive` на светофорах и в пробках, после чего через ~4 минуты поездка автоматически прерывалась, хотя машина продолжала ехать. В длинных поездках одна запись разбивалась на 5+ сегментов. Motion-ended больше не триггерит auto-stop — эта ответственность остаётся только за GPS-скоростью (`<2 км/ч` непрерывно 20 минут) и реальным отключением Bluetooth
- **Pending-таймер авто-стопа отменяется, если машина снова поехала** — после prompt "Машина не движется" 3-минутный таймер автозавершения теперь сбрасывается при возврате активности `automotive`

### Изменено
- **Тексты уведомлений об автозавершении** — больше не пишем "Bluetooth отключился" для всех случаев. BT-disconnect → "Bluetooth отключился. Автозавершение через N мин"; 20-мин неподвижность → "Машина не движется. Автозавершение через N мин"
- **Определение новой поездки в remind-режиме** — флаг "уже показывал reminder" сбрасывается при 10+ минутах неактивности, реальном BT-disconnect или автостопе. Раньше сброс был только по 60-сек debounce motion-ended, который больше не используется
- Версия 0.4.2 → 0.4.3, build 8 → 9

---

## [0.4.2] -- 17 апреля 2026

### Исправлено
- **Remind-режим** — уведомление "Начать запись?" приходит один раз за поездку, а не каждые 5 минут
- **Auto-режим Live Activity** — если запись стартует из бэкграунда, Live Activity появляется при выходе на foreground
- **Восстановление записи после перезапуска** — если приложение было убито iOS во время записи, поездка продолжается при следующем запуске (orphan trips моложе 1 часа восстанавливаются вместо закрытия)

### Добавлено
- **Live Activity на Apple Watch** — Smart Stack отображает скорость и дистанцию во время записи (iOS 18+)
- **Уведомление при автозавершении** — "Поездка завершена: X км, Y мин" приходит на iPhone и Watch
- **Кнопка "Остановить"** в уведомлении об автостарте — можно отменить нежелательную запись с часов

### Изменено
- Версия 0.4.1 → 0.4.2, build 7 → 8

---

## [0.4.1] -- 16 апреля 2026

### Добавлено
- **Sign in with Apple** — кнопка входа через Apple ID в профиле. Credentials хранятся в Keychain. Подготовка к серверному синку
- **KeychainHelper** — обёртка над Security framework для безопасного хранения credentials
- **AuthService** — сервис авторизации: Sign in flow, credential state check на запуске, sign out
- **Два состояния профиля** — Guest (кнопка Sign in внутри аватар-карточки) / Signed In (имя, email, индикатор синка)
- Sign out с подтверждением — данные остаются на устройстве

### Изменено
- Версия 0.4.0 → 0.4.1, build 6 → 7

---

## [0.4.0] -- 15 апреля 2026

### Добавлено
- **CoreData Model Versioning** -- модель данных версионирована (v1 → v2). Все будущие миграции схемы безопасны для существующих пользователей (lightweight migration)
- **Подготовка к серверному синку** -- новые поля на сущностях: `userId`, `serverCreatedAt`, `conflictVersion` (TripEntity), `remoteURL`, `uploadStatus` (TripPhotoEntity), `userId` (VehicleEntity)
- **Codable на моделях** -- `Trip`, `TrackPoint`, `TripPhoto`, `Vehicle` сериализуются в JSON для будущего API
- **TripRepository** -- protocol-абстракция над CoreData CRUD. TripManager делегирует хранение в repository, что позволит подключить серверный синк без переписывания ViewModel'ей
- **User Identity** -- локальный `userId` (UUID) генерируется при первом запуске и проставляется на все существующие сущности. Фундамент для привязки данных к аккаунту
- **Photo Sync Readiness** -- `PhotoUploadStatus` enum (localOnly/uploading/uploaded/failed), `RemotePhotoStorage` protocol (stub), методы `pendingUploads()` и `markUploaded()` в PhotoStorageService
- **SyncQueue** -- очередь синк-операций с дедупликацией, приоритизацией (metadata → photos), exponential backoff retry, интеграция с NetworkMonitor
- **Тесты** -- `CodableRoundTripTests` (6 тестов на сериализацию), `SyncQueueTests` (6 тестов на очередь)

### Изменено
- TripManager CRUD-методы делегируют в TripRepository (−280 строк)
- `thumbnailDiskURL` теперь проверяет path traversal (как `safePhotoURL`)
- SyncQueue помечен `@MainActor` для thread safety
- Все CoreData fetch-запросы имеют `fetchBatchSize` для оптимизации памяти
- `PhotoStorageService.markUploaded` использует `PersistenceController.save()` вместо raw `context.save()`
- Версия 0.3.1 → 0.4.0, build 5 → 6

---

## [0.3.1] -- 13 апреля 2026

### Добавлено
- **Онбординг автозаписи** -- 4-я страница при первом запуске: объяснение зачем нужны фоновая геолокация и датчик движения, кнопка «Включить» запрашивает все разрешения и активирует режим «Напоминание». Можно пропустить и настроить позже
- **Детекция вождения через CMMotion** -- `CMMotionActivityManager` определяет `.automotive` activity в реальном времени (foreground) и ретроспективно (background wake)
- **GPS watchdog** -- если за 60 секунд не приходит ни одного валидного GPS-обновления, трекинг автоматически перезапускается
- **GPS warm-up** -- первые 2 секунды после переключения в режим записи данные игнорируются (устранение скачков скорости)
- **Автозавершение по неподвижности** -- если скорость < 2 км/ч непрерывно 20 минут, предложение завершить поездку
- **Восстановление начала поездки** -- при автостарте через CMMotion запрашиваем реальное время начала `.automotive` activity и бэкдэйтим `startDate` поездки
- **Background task** -- `beginBackgroundTask` при автостарте из фона, чтобы iOS не усыпил приложение пока GPS прогревается

### Изменено
- GPS точность в режиме записи: `kCLLocationAccuracyBest` → `kCLLocationAccuracyBestForNavigation` (добавляет акселерометр + гироскоп)
- Строгая фильтрация GPS: отклонение `speed < 0`, `accuracy > 50м`, stale > 10с при записи
- Notification categories перерегистрируются при смене языка приложения
- `LocationManager.startTracking()` идемпотентен (повторный вызов безопасен)
- Версия 0.3.0 → 0.3.1, build 4 → 5

### Исправлено
- Скачки скорости (0, 50, 20, 33 км/ч) в первые 10 секунд после автостарта записи
- «Мёртвая запись» (0 км после 10+ км) при автостарте из фона и блокировке экрана
- `locationManagerDidPauseLocationUpdates` принудительно возобновляет GPS если идёт запись

---

## [0.3.0] -- 10 апреля 2026

### Добавлено
- **Автозапись поездок по Bluetooth** -- привязка BT-устройства (магнитолы) к машине в деталке автомобиля. Два режима: "Напоминание" (уведомление с кнопкой "Начать запись") и "Авто" (запись стартует автоматически)
- **Детекция вождения через CMMotion** -- `CMMotionActivityManager` определяет `.automotive` activity. Работает после force-quit через `significantLocationChanges` (требует Always location)
- **Трёхслойная детекция**: (1) Audio route -- мгновенная при подключении к BT, app в памяти; (2) CMMotion + significant location -- после force-quit, задержка 1-3 мин; (3) Проверка при разблокировке через `didBecomeActive`
- **Привязка BT-устройства к конкретной машине** -- при подключении к магнитоле автоматически выбирается нужный vehicle
- **Несколько BT-устройств на машину** -- можно привязать и магнитолу, и другие устройства
- **BLE-сканер устройств** (`BluetoothScanSheet`) -- поиск Bluetooth-устройств поблизости с индикатором силы сигнала (цветные бары), сортировка A-Z
- **Уведомления с action buttons** -- "Начать запись" / "Пропустить" на lock screen. Тап по уведомлению = старт записи + переход на экран Record
- **Автозавершение поездки** -- при отключении BT или окончании `.automotive` activity, таймер автозавершения (1-10 мин, настраивается). Debounce 60 секунд для CMMotion (светофоры, пробки)
- **Карточка Bluetooth в деталке машины** -- toggle включения, описание режимов, список привязанных устройств, настройка таймаута автозавершения
- **NotificationManager** -- локальные уведомления с категориями и action buttons
- **AudioRouteDetector** -- мониторинг `AVAudioSession.routeChangeNotification` для Classic Bluetooth (A2DP/HFP)
- **BluetoothDetector** -- `CBCentralManager` с state restoration для BLE-устройств
- **MotionDetector** -- обёртка над `CMMotionActivityManager` с live updates и historical query
- Новые permissions: `NSBluetoothAlwaysUsageDescription`, `NSMotionUsageDescription`, `bluetooth-central` background mode

### Изменено
- `MapViewModel.startRecording()` / `stopRecording()` стали public для вызова из AutoTripService
- Версия 0.2.0 → 0.3.0, build 3 → 4
- `LanguageManager` -- добавлен `static var currentLanguage` для использования вне SwiftUI views
- `SettingsManager` -- добавлены `autoRecordMode`, `autoStopTimeout`, `savedBluetoothDevices` (UserDefaults)

---

## [0.2.0] -- 9 апреля 2026

### Добавлено
- **Fog of War 2.0** -- полная переработка рендеринга тумана: мягкие радиальные градиенты вместо острых прямоугольников, тёмный полупрозрачный туман (`UIColor(white: 0.12, alpha: 0.75)`), органические формы повторяющие маршруты
- **Анимация рассеивания тумана** -- при записи поездки туман плавно рассеивается за 0.7с с easeOut кривой когда заезжаешь на новую территорию (CADisplayLink + setNeedsDisplay)
- **Temporal fog** -- на экране деталки поездки туман показывает состояние на момент завершения поездки (`visitedHashes(before: trip.endDate)`)
- **Экран детальной информации о машине** (`VehicleDetailView`) -- тап на карточку машины в профиле: большой аватар, редактирование имени, одометр с прогресс-баром до следующего уровня, сетка всех 10 стикеров (полученные/заблокированные), выбор аватара (pixel cars + emoji), настройки расхода топлива, единицы измерения, дата создания
- Смена аватара машины (`SettingsManager.updateVehicleAvatar`)
- Кнопка "Подробнее" в меню три точек в гараже -- переход в деталку любой машины
- Тап на активную машину в гараже открывает её деталку
- Лимит 5 машин в гараже с подписью "Максимум 5 автомобилей"
- Сортировка машин в гараже по пробегу (убывание)
- Адаптивный зум на карте регионов -- ближе при малом количестве тайлов, дальше при большом (`min(150km, max(5km, tiles * 350))`)
- Тёмный fog-фон при overscroll на экране деталки поездки

### Изменено
- Настройки расхода топлива и единиц измерения перенесены из гаража в деталку машины
- Рендерер тумана использует пространственное индексирование по geohash4 префиксам для эффективного поиска видимых центров
- Убран компас с карт деталки поездки и регионов
- Убран parallax-stretch на карте деталки поездки
- Отключен bounce у ScrollView на экране деталки поездки
- Pull-to-refresh на экране регионов больше не дёргает заголовок (батчинг state updates + задержка dismiss)

### Исправлено
- Туман не пропадает при старте/остановке записи
- Туман корректно пересобирается после завершения поездки с учётом новых тайлов
- Потокобезопасность: анимация тумана на главном потоке, рендеринг на фоновом потоке MapKit -- данные изолированы через prefix index snapshot

### Удалено
- Старый `FogPolygon` / `FogMaskGenerator` / `FogOverlayRenderer` из `Views/Regions/` -- заменены единым `FogPolygonBuilder.swift`

---

## [0.1.1] -- 7 апреля 2026

### Исправлено
- Убрана демо-поездка при первом запуске — чистый старт без фейковых данных. Существующие демо-поездки удаляются автоматически
- Смягчён алгоритм удаления GPS-аномалий — меньше ложных срабатываний на кривых дорогах и развязках
- Исправлена иконка Telegram в профиле — заменена на чистый paper plane без внешнего круга
- Дефолтная машина переименована из «Телега» в «Ваша машина» / «Your car»
- Переключение между машинами в гараже работает по всей области кнопки
- Карта регионов сразу в правильной теме (тёмная/светлая) без захода на экран записи
- Удаление поездки теперь физически удаляет данные и очищает территории
- Тактильный отклик при остановке записи срабатывает надёжнее (в т.ч. из Live Activity)
- Превью маршрута в ленте теперь всегда соответствует реальному маршруту (исправлена сортировка точек)
- Города в Regions определяются по geocode cache координат, а не по парсингу заголовка поездки

### Изменено
- «Author» → «Developer» в профиле
- Ранг «Novice» переименован в «Beginner»
- Расширены описания в онбординге — упоминание статистики, расхода топлива и гаража
- Версия в профиле читается из Bundle (не хардкод)
- Дефолтная валюта топлива: € вместо ₽
- Улучшен рендеринг тумана на карте регионов — мягче переходы, нет зеленоватого оттенка
- В Regions убрана фиксированная площадь — теперь только исследованные км²
- Проценты исследования в кружках городов и регионов теперь со знаком %
- Убраны стрелки-chevron у городов и регионов (нет перехода)
- «Most explored» вместо вводящего в заблуждение процента от случайного региона
- Оптимизирована производительность маршрута на карте — группировка по скоростным зонам вместо отдельных сегментов

### Добавлено
- Тап по логотипу в ленте — скролл наверх
- Повторный тап по вкладке «Лента» из деталки поездки — возврат назад
- Статистика в профиле (серия, км, поездки) теперь кликабельна — открывает экран статистики
- Быстрая статистика в ленте (trips, km, time) теперь кликабельна — открывает расширенную статистику
- Разрывы GPS на маршруте больше не соединяются прямыми линиями — пустоты вместо артефактов
- Валюта топлива сохраняется per-trip — при смене валюты старые поездки помнят свою
- Pull-to-refresh на экране Regions
- Кеширование данных Regions — мгновенный повторный вход на экран

---

## [0.1.0] -- 24 марта 2026

Первый релиз. Полнофункциональный автодневник поездок.

### Добавлено

**Запись маршрута**
- GPS-трекинг с фильтром Калмана (`SmoothTrackManager`) для сглаживания координат и удаления шума
- Фоновая запись маршрута (`UIBackgroundModes: location`)
- Реалтайм-показатели во время записи: текущая скорость, высота, пройденная дистанция, время
- Сворачиваемая панель метрик (compact/expanded `TrackingHUD`) -- тап переключает между режимами
- Компактный HUD (`CompactTrackingHUD`) с минимальной информацией
- Спидометр (`SpeedometerView`) с визуальной шкалой
- Индикатор качества GPS-сигнала (`GPSIndicatorView`)
- Плавная анимация "головы" маршрута с glow-эффектом (`GlowingHeadOverlay`)
- Автоматический zoom карты по скорости движения (ближе на малых скоростях, дальше на высоких)
- Пауза и возобновление записи
- Автоматическая темная/светлая тема карты по времени суток (`SunCalculator` -- расчет sunrise/sunset по координатам)
- Idle-экран с пульсирующей кнопкой старта, суммарным пробегом и количеством поездок
- Экран итогов после завершения поездки (`TripCompleteSummaryView`): карта маршрута, статистика, заработанные XP, полученные бейджи, кнопки добавления фото и заметок

**Лента поездок**
- Группировка поездок по месяцам с collapsible-секциями
- Contribution calendar -- GitHub-style heatmap с интенсивностью по километрам за день
- Фильтрация по дате через calendar picker (выбор диапазона)
- Фильтрация по региону (чипы уникальных регионов из геокодинга)
- Поиск по названию поездки
- Быстрая статистика в header: общий пробег, количество поездок, суммарное время
- Пагинация -- 20 поездок на страницу, подгрузка при скролле
- Карточки поездок с превью маршрута (`LightRoutePreview`), названием, датой, дистанцией, временем
- Swipe-удаление с undo-таймером (5 секунд) и toast-уведомлением
- Skeleton-загрузка карточек при первом открытии
- Empty state с предложением начать первую поездку
- Подробная статистика (`StatsView`): текущий/прошлый месяц, недельная активность (Swift Charts), calendar heatmap, топ мест, рекорды

**Детали поездки**
- Интерактивная карта с маршрутом и parallax-stretch при скролле
- Маршрут раскрашен градиентом по скорости (`RouteMapView`)
- Статистика: дистанция, время, средняя/максимальная скорость, набор высоты
- Фотографии поездки -- добавление через `PhotoPickerView`, полноэкранный просмотр (`PhotoFullScreenView`), удаление
- Редактирование названия поездки (inline edit с фокусом)
- Автоматический геокодинг: название формируется из начальной и конечной точек. При ошибке -- retry
- Share-карточка (`TripShareCardView`) для Instagram/соцсетей: маршрут + статистика + брендинг
- Добавление текстовых заметок (`NotesEditorView`)
- Бейджи, заработанные за поездку (`TripBadgesRow`)

**Карта территорий**
- Scratch-карта (fog of war) на основе geohash precision-6 (`GeohashEncoder` + `VisitedGeohashEntity`)
- Fog-маска генерируется на GPU (`FogMaskGenerator`, `FogOverlayRenderer`)
- Fullscreen-режим карты (`FullscreenFogMapView`)
- Статистика: количество посещенных тайлов
- Списки посещенных городов и регионов с количеством визитов
- Все маршруты всех поездок отображаются как polyline overlay
- Backfill при первом запуске -- обрабатывает существующие поездки

**Геймификация**
- 30+ бейджей в 4 категориях: дистанция, исследование, особые, серии
- Бейджи дистанции: Первый путь, Завсегдатай (10 поездок), Дорожный воин (50), километровые вехи (1/100/1000/10000 км), Марафонец (42.2 км за поездку), Стоик (100+ км за поездку), Железная задница (500+ км за поездку), Спид-демон (120+ км/ч)
- Скрытые бейджи (`isHidden`) -- не видны до получения
- Повторяемые бейджи (`isRepeatable`) -- можно получить несколько раз
- Celebration-экран с конфетти при получении бейджа (`BadgeCelebrationView`, `ConfettiView`)
- Детальный overlay для каждого бейджа (`BadgeDetailOverlay`)
- Каталог всех бейджей (`BadgesView`) с группировкой по категориям
- XP-система: 1 XP за километр, x2 бонус за поездки 200+ км, +20 XP за первую поездку дня, +50 XP за новый регион, x1.5 за поездку в новом регионе
- Уровни водителя (1-30) с прогрессивной шкалой XP (`LevelSystem`)
- Ранги: Новичок (1-4), Водитель (5-9), Путешественник (10-14), Исследователь (15-19), Навигатор (20-24), Дальнобойщик (25-29), Легенда (30)
- Daily streak -- серия дней с поездками
- Профиль (`ProfileView`) с выбором аватара (8 эмодзи), карточкой уровня (`DriverLevelView`), быстрой статистикой

**Коллекция дорог**
- Автоматическое обнаружение часто проезжаемых дорог (`RoadCollectionManager`)
- Уровни освоения дороги (discovered, familiar, mastered)
- Карточки дорог с информацией о редкости
- Фильтрация по редкости

**Гараж**
- Профиль автомобиля с одометром (`VehicleCardView`)
- Экран управления машинами (`GarageView`)

**Интерфейс**
- Кастомный glass tab bar (`CustomTabBar`) -- 3 вкладки: лента, запись, территории
- Центральная кнопка записи (оранжевая пульсация в idle, красный стоп при записи)
- UI-модификаторы для единообразия карточек: `.surfaceCard()`, `.glassBackground()`, `.glassPill()`
- Адаптивная цветовая палитра (`AppTheme`) для светлой и темной темы
- Toast-уведомления (`ToastView`)
- Баннер записи (`RecordingBanner`) -- видно на всех вкладках
- Haptic feedback на все интерактивные элементы

**Настройки**
- Тема оформления: системная / светлая / темная (`ThemeManager`)
- Язык интерфейса: русский / английский -- переключается на лету без перезапуска (`LanguageManager`)
- Все строки UI через `AppStrings` enum -- нет hardcoded текста
- Отображение версии и номера сборки из bundle

**Онбординг**
- 3 страницы при первом запуске: приветствие, описание записи, запрос разрешения на геолокацию
- Swipe между страницами (TabView с page style)
- Кнопка "Поехали!" на финальной странице запрашивает location permission

**Данные и persistence**
- CoreData с 8 entities: `TripEntity` (центральная), `TrackPointEntity`, `TripPhotoEntity`, `VehicleEntity`, `UserSettingsEntity`, `VisitedGeohashEntity`, `RoadEntity`
- Cascade-удаление: удаление поездки удаляет все track points и фото
- Batch saves: точки GPS батчатся по 10 штук или каждые 15 секунд (`saveBatchSize`, `saveInterval`)
- Асинхронное сохранение (`PersistenceController.saveAsync()`) для неблокирующих записей из location callbacks
- Binary polyline encoding (`Trip.encodePolyline/decodePolyline`) -- компактное хранение маршрута в Float32 парах
- Превью-полилайн для карточек ленты (упрощенный маршрут)
- Автоудаление мусорных поездок: <500 метров AND <2 минуты
- Очистка orphaned trips (без endDate) при старте
- Фото хранятся в Documents directory (`PhotoStorageService`), миниатюры через `AsyncThumbnailView`

**Разработка**
- Provider pattern для location: `LocationProvider` протокол, `RealGPSProvider` (CoreLocation) + `SimulatedLocationProvider` (dev-джойстик)
- Dev mode с виртуальным джойстиком для тестирования без реального GPS
- Dev menu (`DevMenuView`) для переключения режимов
- Мониторинг сети (`NetworkMonitor`)
- Сборка через `xcodegen` (`project.yml`), локальный signing через `Local.xcconfig` (gitignored)
- Нулевые внешние зависимости -- 100% нативные фреймворки Apple
